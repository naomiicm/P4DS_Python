{"nbformat":4,"nbformat_minor":0,"metadata":{"colab":{"provenance":[],"authorship_tag":"ABX9TyNRumO7MRwCt4YMNcBT1W+H"},"kernelspec":{"name":"python3","display_name":"Python 3"},"language_info":{"name":"python"}},"cells":[{"cell_type":"code","source":["import numpy as np\n","import pandas as pd\n","import math\n","import cmath as im\n","import statistics as st\n","import random\n","import copy\n","\n","from datetime import date"],"metadata":{"id":"P-bDsNFtRSFZ","executionInfo":{"status":"ok","timestamp":1709593748337,"user_tz":-60,"elapsed":254,"user":{"displayName":"Naomi Cedeño Manrique","userId":"07623801168020297088"}}},"execution_count":3,"outputs":[]},{"cell_type":"markdown","source":["Write a Python program that reads a string in input from\n","the user, convert it into a tuple of characters, and first print\n","the characters in even position and then the ones in odd\n","position:"],"metadata":{"id":"pUReicBbDPpP"}},{"cell_type":"code","source":["text = \"hey how are you\"\n","\n","p = (text[0], )\n","\n","for i in text[1:len(text)]:\n","  p += (i, )\n","\n","print(f'Even position: {[p[i] for i in range(len(p)) if i % 2 == 0]}')\n","print(f'Odd position: {[p[i] for i in range(len(p)) if i % 2 != 0]}')"],"metadata":{"id":"L_5wetb5DPQH"},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":["\n","Write a Python program to remove an item from a tuple:"],"metadata":{"id":"5t_fimM_DRoU"}},{"cell_type":"code","source":["tup = (1, \"s\", [23, 4], 3)\n","elem = [23, 4]\n","\n","tup = list(tup)\n","\n","if elem in tup:\n","  tup.remove(elem)\n","else:\n","  print(\"Object not found, tuple remains the same.\")"],"metadata":{"id":"qksO_r7YDUPe"},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":["Write a Python program to find the index of an item of a\n","tuple:"],"metadata":{"id":"vwnBwK_oDU3X"}},{"cell_type":"code","source":["tup = (1, \"s\", [23, 4], 3)\n","elem = \"s\"\n","\n","if elem in tup:\n","  print(f'Index of element {elem}: {tup.index(elem)}')\n","else:\n","  print(\"Object not found in the tuple.\")"],"metadata":{"id":"KNXhVRabDYHp"},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":["Write a function that prints all pair combinations of 2 tuples\n","element-wise:"],"metadata":{"id":"3hdk_v0HDanQ"}},{"cell_type":"code","source":["tup_1 = (1, \"a\", True)\n","tup_2 = (2, \"b\", False)\n","\n","for i in tup_1:\n","  for j in tup_2:\n","    print(f\"Combination: ({i}, {j})\")"],"metadata":{"id":"V2qavLfLDdYw"},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":["A complex number is a number of the form $a + bi$, whereby $a$ and $b$ are constants, and $i$ is a special value that is defined as $\\sqrt{-1}$. Represent a complex number as a tuple of two numeric values, and create a function that calculates the addition of two complex numbers:"],"metadata":{"id":"jfV81QEFKBpM"}},{"cell_type":"code","source":["def sum_cpx(c1, c2):\n","  real = (c1[0] + c2[0])\n","  cpx = (c1[1] + c2[1])\n","\n","  print(f\"({c1[0]} + {c1[1]}i) + ({c2[0]} + {c2[1]}i) =  {real} + {cpx}i\")"],"metadata":{"id":"loPYj8__KYry"},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":["Multiplication of two complex numbers $a + bi$ and $c + di$ is defined as\n","$(ac - bd)$ + $(ad + bc)i$. Write a function that calculates the multiplication of two complex numbers:"],"metadata":{"id":"tWBM6EEKKZFP"}},{"cell_type":"code","source":["def prod_cpx(c1, c2):\n","  real = (c1[0] * c2[0]) - (c1[1] * c2[1])\n","  cpx = (c1[0] * c2[1]) + (c1[1] * c2[0])\n","\n","  print(f\"({c1[0]} + {c1[1]}i) * ({c2[0]} + {c2[1]}i) =  {real} + {cpx}i\")"],"metadata":{"id":"XhtNqLSPKhgy"},"execution_count":null,"outputs":[]}]}